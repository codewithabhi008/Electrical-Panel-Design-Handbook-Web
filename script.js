const themeBtn=document.getElementById("themeBtn");const body=document.body;const saved=localStorage.getItem("panel-theme");if(saved==="light"){body.classList.add("light");if(themeBtn)themeBtn.textContent="🌙"}if(themeBtn)themeBtn.addEventListener("click",()=>{body.classList.toggle("light");const light=body.classList.contains("light");localStorage.setItem("panel-theme",light?"light":"dark");themeBtn.textContent=light?"🌙":"☀️"});const search=document.getElementById("search");const cards=[...document.querySelectorAll(".card")];const noResults=document.getElementById("noResults");if(search){search.addEventListener("input",()=>{const q=search.value.toLowerCase().trim();let shown=0;cards.forEach(c=>{const ok=c.dataset.key.includes(q);c.style.display=ok?"block":"none";if(ok)shown++});if(noResults)noResults.style.display=shown?"none":"block"})}